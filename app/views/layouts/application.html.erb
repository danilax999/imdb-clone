<!DOCTYPE html>
<html>
  <head>
    <title>ImdbClone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <%= render 'shared/navbar' %>
    <div class="bg-main w-100 d-flex flex-column align-items-center">
      <main class="main d-flex flex-column align-items-center w-100" style="max-width: 800px;">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
