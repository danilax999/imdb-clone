<nav class="navbar navbar-light navbar-expand-md bg-light">
  <div class="container-fluid">
    <div class="navbar-nav d-flex flex-row w-100 justify-content-between">
      <div id="nav-left" class="d-flex flex-row align-items-center">
        <%= link_to 'Movies', movies_path, class: 'text-decoration-none text-reset px-1 py-2 mx-1' %>
      </div>
      <div id="nav-center"></div>
      <div id="nav-right">
        <% if user_signed_in? %>
          <%# button_to 'Log out', destroy_user_session_path, method: :delete, class: 'btn btn-primary' %>
          <div class="nav-item dropdown">
            <a id="nav-profile-menu" class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false" >
              Profile
            </a>
              <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="nav-profile-menu">
              <li>
                <%= link_to 'Edit', edit_user_registration_path, class: 'dropdown-item' %>
              </li>
              <li>
                <%= button_to 'Log out', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
              </li>
            </ul>
          </div>
        <% else %>
          <%= link_to 'Sign in', new_user_session_path, class: 'btn btn-secondary mx-1' %>
          <%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-primary mx-1' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
